const photoAddApiUrl = "https://localhost:7777/api/photo/add"

export default () => {

    let uploadButton = document.getElementById('btn-upload-image');
    uploadButton.addEventListener('click', () => {

    });
}